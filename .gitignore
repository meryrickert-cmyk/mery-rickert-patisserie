# Dependencias
node_modules/
client/node_modules/
server/node_modules/

# Build
client/dist/

# Base de datos (queda en el volumen de Railway)
# server/mery.db ← temporalmente incluida para migración inicial
server/mery.db-shm
server/mery.db-wal
# server/uploads/ ← temporalmente incluido para migración inicial al volumen

# Variables de entorno — nunca subir al repo
server/.env
.env

# Sistema operativo
.DS_Store
Thumbs.db
server/mery.db
